<div class='card'>
  <div class='card-body'>

    <div
      class='display-4 text-muted {{position}}'
    >{{data.title}}</div>
    <h6 class={{position}}>{{data.sub_title}}</h6>
    <p class={{position}}>{{data.description}}</p>
    <button class='btn-outline-warning btn'>
      {{data.button}}</button>
  </div>
</div>